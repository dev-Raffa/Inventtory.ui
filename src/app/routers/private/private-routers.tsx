import { SystemLayout } from "@/app/layouts/system/system-layout"
import { Route } from "react-router"

export const PrivateRouters = ()=>{
    return(    
        <Route element={<SystemLayout />}>

        </Route>
        
    )
}